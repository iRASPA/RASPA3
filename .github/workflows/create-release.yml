name: create-binary-packages
on:
  workflow_dispatch:
    branches: [ "main" ]
    inputs:
      tag-name:
        description: 'Name of the tag that will be used for this release'
        required: true
      version:
        description: 'Code version passed to cmake.'
        required: true

jobs:
  release-job:
    permissions: write-all
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.step_upload_url.outputs.upload_url }}
    steps:
    - name: checkout-code
      uses: actions/checkout@v6
    - name: conventional-changelog-action
      uses: TriPSs/conventional-changelog-action@v6
      id: changelog
      with:
        create-summary: 'true'
        skip-bump: 'true'
        skip-commit: 'true'
        skip-tag: 'true'
        output-file: 'false'
        github-token: ${{ secrets.github_token }}
    - name: create-release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.event.inputs.tag-name }}
        release_name: Release ${{ github.event.inputs.tag-name }}
        body: ${{ steps.changelog.outputs.clean_changelog }}
        draft: false
        prerelease: false
    - name: set-upload_url-output
      id: step_upload_url
      run: echo "upload_url=${{ steps.create_release.outputs.upload_url }}" >> "$GITHUB_OUTPUT"

# x86_64 ubuntu-24 pre-avx2 legacy packages
# =====================================================================================================================
#
  linux-x86_64-corei7-ubuntu-24-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-24-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-corei7-ubuntu-24
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-24-corei7.deb
          asset_content_type: application/octet-stream


  linux-x86_64-corei7-avx-ubuntu-24-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-24-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-corei7-avx-ubuntu-24
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-24-corei7-avx.deb
          asset_content_type: application/octet-stream

# x86_64 linux avx2
# =====================================================================================================================
#
  
  linux-x86_64-core-avx2-ubuntu-26-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-26-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-ubuntu-26
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-26-core-avx2.deb
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-ubuntu-24-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-24-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-ubuntu-24
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-24-core-avx2.deb
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-ubuntu-22-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-22-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-ubuntu-22
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-22-core-avx2.deb
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-ubuntu-20-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-20-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-ubuntu-20
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-20-core-avx2.deb
          asset_content_type: application/octet-stream


  linux-x86_64-core-avx2-debian-13-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-13-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-debian-13
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-13_core-avx2.deb
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-debian-12-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-12-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-debian-12
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-12_core-avx2.deb
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-debian-11-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-11-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-debian-11
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-11_core-avx2.deb
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-debian-10-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-10-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-debian-10
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-10_core-avx2.deb
          asset_content_type: application/octet-stream


  linux-x86_64-core-avx2-rocky-10-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-10-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-redhat-10
      - name: create-raspa-rpm-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el10.x86_64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el10.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el10.x86_64.core-avx2.rpm
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-rocky-9-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-9-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-redhat-9
      - name: create-raspa-rpm-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el9.x86_64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el9.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el9.x86_64.core-avx2.rpm
          asset_content_type: application/octet-stream

  linux-x86_64-core-avx2-rocky-8-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-8-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-redhat-8
      - name: create-raspa-rpm-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el8.x86_64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el8.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el8.x86_64.core-avx2.rpm
          asset_content_type: application/octet-stream


  linux-x86_64-core-avx2-rocky-7-job:
    runs-on: ubuntu-latest
    needs: release-job
    steps:
      - name: configure-cmake-raspa
        uses: addnab/docker-run-action@v3
        with:
          image: ghcr.io/iraspa/rocky-7-x86_64-llvm-22:latest
          options: -v ${{ github.workspace }}:/tmp2
          run: |
            git clone https://github.com/iraspa/raspa3
            cd raspa3
            cmake -B ${{github.workspace}}/build --preset linux-x86_64-core-avx2-redhat-7
            ninja -C ${{github.workspace}}/build package
            cp ${{github.workspace}}/build//raspa-${{ github.event.inputs.version }}-1.el7.x86_64.rpm /tmp2
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el7.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el7.x86_64.core-avx2.rpm
          asset_content_type: application/octet-stream



# x86_64 linux avx512
# =====================================================================================================================

  linux-x86_64-skylake-avx512-ubuntu-26-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-26-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-ubuntu-26
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-26_skylake-avx512.deb
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-ubuntu-24-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-24-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-ubuntu-24
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-24_skylake-avx512.deb
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-ubuntu-22-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-22-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-ubuntu-22
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-22_skylake-avx512.deb
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-ubuntu-20-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-20-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-ubuntu-20
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_ubuntu-20_skylake-avx512.deb
          asset_content_type: application/octet-stream


  linux-x86_64-skylake-avx512-debian-13-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-13-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: | 
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-debian-13
      - name: create-raspa-deb-package
        shell: bash
        run: | 
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-13_skylake-avx512.deb
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-debian-12-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-12-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: | 
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-debian-12
      - name: create-raspa-deb-package
        shell: bash
        run: | 
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-12_skylake-avx512.deb
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-debian-11-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-11-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: | 
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-debian-11
      - name: create-raspa-deb-package
        shell: bash
        run: | 
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-11_skylake-avx512.deb
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-debian-10-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-10-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v3
      - name: configure-cmake-raspa
        shell: bash
        run: | 
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-debian-10
      - name: create-raspa-deb-package
        shell: bash
        run: | 
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_amd64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_amd64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_amd64_debian-10_skylake-avx512.deb
          asset_content_type: application/octet-stream


  linux-x86_64-skylake-avx512-rocky-10-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-10-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }} 
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-redhat-10
      - name: create-raspa-rpm-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el10.x86_64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el10.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el10.x86_64.skylake-avx512.rpm
          asset_content_type: application/octet-stream
      
  linux-x86_64-skylake-avx512-rocky-9-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-9-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-redhat-9
      - name: create-raspa-rpm-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el9.x86_64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el9.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el9.x86_64.skylake-avx512.rpm
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-rocky-8-job:
    runs-on: ubuntu-latest
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-8-x86_64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-redhat-8
      - name: create-raspa-rpm-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el8.x86_64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el8.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el8.x86_64.skylake-avx512.rpm
          asset_content_type: application/octet-stream

  linux-x86_64-skylake-avx512-rocky-7-job:
    runs-on: ubuntu-latest
    needs: release-job
    steps:
      - name: configure-cmake-raspa
        uses: addnab/docker-run-action@v3
        with:
          image: ghcr.io/iraspa/rocky-7-x86_64-llvm-22:latest
          options: -v ${{ github.workspace }}:/tmp2
          run: |
            git clone https://github.com/iraspa/raspa3
            cd raspa3
            cmake -B ${{github.workspace}}/build --preset linux-x86_64-skylake-avx512-redhat-7
            ninja -C ${{github.workspace}}/build package
            cp ${{github.workspace}}/build//raspa-${{ github.event.inputs.version }}-1.el7.x86_64.rpm /tmp2
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el7.x86_64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el7.x86_64.skylake-avx512.rpm
          asset_content_type: application/octet-stream



# aarch64 linux 
# =====================================================================================================================
  
  linux-aarch64-ubuntu-26-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-26-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-ubuntu-26
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_arm64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_arm64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_arm64_ubuntu-26.deb
          asset_content_type: application/octet-stream

  linux-aarch64-ubuntu-24-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-24-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-ubuntu-24
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_arm64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_arm64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_arm64_ubuntu-24.deb
          asset_content_type: application/octet-stream

  linux-aarch64-ubuntu-22-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/ubuntu-22-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-ubuntu-22
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_arm64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_arm64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_arm64_ubuntu-22.deb
          asset_content_type: application/octet-stream

  linux-aarch64-debian-13-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-13-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-debian-13
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_arm64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_arm64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_arm64_debian-13.deb
          asset_content_type: application/octet-stream

  linux-aarch64-debian-12-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/debian-12-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-debian-12
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa_${{ github.event.inputs.version }}_arm64.deb .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa_${{ github.event.inputs.version }}_arm64.deb
          asset_name: raspa_${{ github.event.inputs.version }}_arm64_debian-12.deb
          asset_content_type: application/octet-stream

  linux-aarch64-redhat-10-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-10-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-redhat-10
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el10.aarch64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el10.aarch64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el10.aarch64.rpm
          asset_content_type: application/octet-stream

  linux-aarch64-redhat-9-job:
    runs-on: ubuntu-24.04-arm
    needs: release-job
    container:
      image: ghcr.io/iraspa/rocky-9-arm64-llvm-22:latest
      credentials:
         username: ${{ github.actor }}
         password: ${{ secrets.github_token }}
    steps:
      - name: checkout-repository
        uses: actions/checkout@v6
      - name: configure-cmake-raspa
        shell: bash
        run: |
          cmake -B ${{github.workspace}}/build --preset linux-aarch64-redhat-9
      - name: create-raspa-deb-package
        shell: bash
        run: |
          ninja -C ${{github.workspace}}/build package
          mv ${{github.workspace}}/build/raspa-${{ github.event.inputs.version }}-1.el9.aarch64.rpm .
      - name: upload-release-asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release-job.outputs.upload_url }}
          asset_path: ./raspa-${{ github.event.inputs.version }}-1.el9.aarch64.rpm
          asset_name: raspa-${{ github.event.inputs.version }}-1.el9.aarch64.rpm
          asset_content_type: application/octet-stream
